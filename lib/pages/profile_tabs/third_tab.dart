import 'package:flutter/material.dart';

class ThirdProfileTab extends StatelessWidget {
  const ThirdProfileTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("third Tab"),
    );
  }
}
